{{- if .Values.ingress.enabled -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "goff-manager.fullname" . }}
  namespace: {{ include "goff-manager.namespace" . }}
  labels:
    {{- include "goff-manager.labels" . | nindent 4 }}
  {{- with .Values.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if .Values.ingress.className }}
  ingressClassName: {{ .Values.ingress.className }}
  {{- end }}
  {{- if .Values.ingress.tls }}
  tls:
    {{- range .Values.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            pathType: {{ .pathType }}
            backend:
              service:
                {{- if eq .service "ui" }}
                name: {{ include "goff-manager.ui.fullname" $ }}
                port:
                  number: {{ $.Values.ui.service.port }}
                {{- else if eq .service "api" }}
                name: {{ include "goff-manager.api.fullname" $ }}
                port:
                  number: {{ $.Values.api.service.port }}
                {{- else if eq .service "relayProxy" }}
                name: {{ include "goff-manager.relayProxy.fullname" $ }}
                port:
                  number: {{ $.Values.relayProxy.service.port }}
                {{- end }}
          {{- end }}
    {{- end }}
{{- end }}
